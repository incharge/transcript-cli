{
  "name": "@incharge/transcript-cli",
  "version": "0.0.4-alpha.5",
  "description": "Work in progress - Command-line interface (CLI) for processing transcripts",
  "author": "Julian Price, inCharge Ltd, UK",
  "license": "ISC",
  "type": "module",
  "main": "./bin/awstoic.js",
  "module": "./bin/awstoic.js",
  "bin": {
    "awstoic": "./bin/awstoic.js",
    "createpages": "./bin/createpages.js"
  },
  "files": [
    "./bin/awstoic.js",
    "./bin/createpages.js"
  ],
  "scripts": {
    "clean": "rm -rf bin",
    "compile": "tsc",
    "link": "npm link @incharge/transcript-core",
    "test": "node ./bin/awstoic.js test/924-aws-pretty.json test/924-ic-test.json",
    "dev": "npm run compile && npm run test",
    "watch": "npx nodemon -e ts --watch src --exec \"npm run compile\" && exit 1",
    "prepublishOnly": "npm run clean && npm run compile"
  },
  "dependencies": {
    "@incharge/transcript-core": "^0.0.4-alpha.2",
    "minimist": "^1.2.8"
  },
  "devDependencies": {
    "@types/minimist": "^1.2.5",
    "@types/node": "^20.12.12",
    "typescript": "^5.4.5"
  }
}
